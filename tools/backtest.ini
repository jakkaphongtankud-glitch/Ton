; ===================================================================
;  Strategy Tester config for MTF_Structure_Scalper_Runner_v2
;  Launch with:  terminal64.exe /config:backtest.ini
;  (see run_backtest.bat)
;
;  NOTE: current build (01-06) places NO orders yet, so the report
;  will show 0 trades. This run only proves the EA loads and runs
;  WITHOUT runtime errors. For real profit backtests, wait for the
;  Core build (Build 18 / v2.0.0_CORE). To eyeball detection, set
;  Visual=1 and InpDebugStructure=true.
; ===================================================================
[Tester]
Expert=MTF_Structure_Scalper_Runner_v2\MTF_Structure_Scalper_Runner_v2_0
Symbol=XAUUSDm
Period=M15
Model=4                 ; 4 = Every tick based on real ticks (best quality)
Optimization=0
FromDate=2024.01.01
ToDate=2025.06.30
ForwardMode=0
Deposit=10000
Currency=USD
Leverage=500
ExecutionMode=0
Report=MTSR2_report
ReplaceReport=1
ShutdownTerminal=1
Visual=0                ; set to 1 to watch structure/zone/bias on the chart
